# Streamlit Dashboard Dependencies
streamlit>=1.28.0
pandas>=1.5.0
numpy>=1.23.0
matplotlib>=3.6.0
plotly>=5.15.0
yfinance>=0.2.0

# Note: The RL model display uses a simplified approximation strategy
# that doesn't require the full RL stack (gymnasium, stable-baselines3, torch)
# The trained model file (portfolio_ppo_fast.zip) is included for reference
